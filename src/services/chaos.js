'use strict';

//
const start = (duration) => {

  // return token
};

const stop = (token) => {

};

// run on schedule
const run () => {
  // remove finished chaos tasks

  // do chaos
};

module.exports = {
  start: start,
  stop: stop,
  run: run
};
